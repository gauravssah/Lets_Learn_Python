# Can we have a set with 18 (int) and '18' (str) as a value in it?

s = {18, '18'}

print(s) # {18, '18'}
# Explain : Yes We can have a set with 18 (int) and '18' (str) as a value in it.

print(type(s)) # <class 'set'>


