# 9. Can you change the values inside a list which is contained in set S?

s = {8, 7, 12, "Harry", [1,2]}

print(type(s))
s[0] = 20 # We can't change the values inside a list which is contained in set S



# Why - We can't change the values inside a list which is contained in set S.
# Because - We can't change the values inside a list which is contained in set S.

